# Telecom CRM - Environment Variables

# The primary database connection for the CRM system
DATABASE_URL=postgres://postgres:admin@localhost:5433/crm_db

# The default administrative database connection used to create the crm_db if it does not exist
DEFAULT_DATABASE_URL=postgres://postgres:admin@localhost:5433/postgres
